{
  "events": [
    {
      "id": "clan_split",
      "name": "clan split",
      "target": "faction",
      "assigners" : ["world"],
      "assignmentConditions": ["target.type == clan"],
      "triggerConditions": [
        "target.administrative_load > 0.5",
        "!(target.preferences.cohesion > 0.7)"
      ],
      "maxTimeToTrigger": "91250 * target.administrative_load * target.preferences.cohesion",
      "effects": [
        "target.trigger_decision(clan_split)"
      ],
      "repeatable" : true
    }
  ]
}
